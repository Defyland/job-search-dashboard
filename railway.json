{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "bundle exec rails db:prepare && bundle exec rails dashboard:seed_sources && bundle exec rails dashboard:bootstrap_admin"
  }
}
